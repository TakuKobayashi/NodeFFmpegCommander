import FFmpegCommander from '../lib/ffmpeg-commander';

describe('FFmpegCommander', () => {
  it('getMetaInfo', () => {
  });

  it('asyncGetMetaInfo', () => {
  });

  it('convertVideoSize', () => {
  });

  it('getVideoSize', () => {
  });

  it('asyncGetVideoSize', () => {
  });

  it('captureThumbnail', () => {
  });

  it('asyncCaptureThumbnail', () => {
  });

  it('createTelopSrtFile', () => {
  });

  it('convertFFmpegTime', () => {
  });

  describe('covertFile', () => {
    it('mp4 to wav', () => {
    });

    it('mp4 to mov', () => {
    });

    it('mp4 to gif', () => {
    });

    it('srt to ass', () => {
    });
  });

  it('asyncCovertFile', () => {
  });

  it('run', () => {
  });

  it('runSync', () => {
  });
})